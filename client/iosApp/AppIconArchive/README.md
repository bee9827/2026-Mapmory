# App icon archive

`legacy-sizes/` contains the previously generated iPhone and iPad icon sizes.
They are kept only as a backup and are not referenced by the Xcode asset catalog.

The active app icon is `Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png`.
Xcode generates the device-specific icon sizes from that 1024×1024 source image.
