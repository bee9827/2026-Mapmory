spring:
  flyway:
    locations:
      - classpath:db/migration
      - classpath:db/local-migration
  datasource:
    url: jdbc:mysql://localhost:3306/mapmory?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: local
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5